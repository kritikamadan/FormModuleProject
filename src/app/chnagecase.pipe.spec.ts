import { ChnagecasePipe } from './chnagecase.pipe';

describe('ChnagecasePipe', () => {
  it('create an instance', () => {
    const pipe = new ChnagecasePipe();
    expect(pipe).toBeTruthy();
  });
});
